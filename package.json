{
  "name": "myapp",
  "version": "1.0.0",
  "description": "my test app for auto update of electron",
  "main": "index.js",
  "scripts": {
    "test": "test",
    "start": "electron .",
    "build": "electron-builder build  --win --publish never",
    "deploy": "electron-builder build --win --publish always"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/hemanth500apps/myapp.git"
  },
  "author": "hemanth",
  "license": "ISC",
  "devDependencies": {
    "electron": "^8.0.1",
    "electron-builder": "^22.3.2"
  },
  "dependencies": {
    "electron-updater": "^4.2.2"
  }
}
